面试官你好，我叫吴泽炜，本科是计算机科学与技术，有9年的一个后端开发经验，主要是做复杂业务建模，核心链路开发，
多系统集成的相关工作。目前在云鲸担任高级开发工程师，主要是负责供应链系统，模具管理系统，低代码表单和流程引擎中台的开发，
同时负责iot部分功能的相关开发，在团队方面，同时需要负责新人带教，以及指导外包同事任务拆解，指导开发，保证稳定交付。
在金蝶的时候，主要是负责RPA流程机器人的云端控制台开发，以及负责部分苍穹GTP平台的相关开发工作。以上就是我的近期工作履历。
然后我平时也比较关注AI相关的技术，我是公司里面第一批使用cursor的员工，比较熟悉cursor和claude code工具的使用，在日常工作中
也是推动指导其他同事一起使用AI工具，提高工作效率，目前我是在职状态，投递这个岗位主要也是前同事的一个推荐，
觉得贵公司的岗位比较符合我的一个工作经历，也了解到贵公司在一个比较快的发展阶段，有较大的发展潜力，所以我这边就想来试一下，
看能不能跟贵公司一起成长。





下面按 **岗位职责（机器人云平台：设备管理、任务调度、数据管理、订单/售后/物流/支付、基础组件、稳定与性能）** 和 **简历里两个重点项目（AIoT、金蝶 RPA+GPT）** 整理：**30 道高频题（简答易背）** + **场景题由浅入深（含答案）**。

---

## 一、30 道高概率面试题（附简答）

### A. 岗位动机与迁移（3）
1. **为什么你能胜任机器人云平台后端？**  
   **答：** 做过设备接入与运维（AIoT）和云控调度（RPA），熟悉接入鉴权、设备档案、任务调度、指令闭环、可观测；差异在协议与安全，但控制面方法论一致。

2. **软件 RPA 和真机机械臂有什么相同与不同？**  
   **答：** 相同：实例管理、调度、日志、开放接口、资源控制。不同：真机更重时延、物理安全、确定性；链路上要更严的权限与审计。

3. **你怎么看重“订单/售后/物流/支付”与你过去经历？**  
   **答：** 我有企业复杂业务与强状态机经验（供应链/资产等）。交易域关键是状态机、幂等、对账与外部回调一致性。

### B. 微服务与边界（3）
4. **设备管理 vs 任务调度要不要拆开？**  
   **答：** 倾向拆：前者管档案与连接状态，后者管队列、优先级、重试与资源；数据与发布节奏不同；通过 API/事件协作。

5. **控制面与交易面怎么避免互相拖垮？**  
   **答：** 隔离线程池/限流、异步化、失败降级；关键链路优先级与熔断；观测分开看。

6. **跨团队集成你最关注什么？**  
   **答：** 契约版本、幂等、回调签名、沙箱联调、错误码语义、对账补偿。

### C. 一致性、幂等、消息（6）
7. **跨服务一致性怎么做？**  
   **答：** 默认最终一致（MQ）；强一致优先单服务事务；跨服务用可靠投递/补偿/对账兜底。

8. **至少一次投递下如何避免重复消费？**  
   **答：** 业务幂等键 + 去重表/唯一索引 + 状态机防重复推进。

9. **Outbox（发件箱）解决什么问题？**  
   **答：** 本地事务与发消息的原子性，避免“库成功消息没发出”或反过来。

10. **MQTT 场景怎么做幂等？**  
    **答：** 设备+msgId/业务序号去重；落库唯一约束；重复上报只更新或忽略并打审计。

11. **HTTP 回调幂等怎么做？**  
    **答：** 回调带 request_id；服务端保存第一次结果；返回相同响应。

12. **开放 API 幂等怎么做？**  
    **答：** `Idempotency-Key` 或业务单号；接口侧限时去重窗口；与状态机结合。

### D. MySQL / Redis（4）
13. **任务表/设备表设计你关注什么？**  
    **答：** 状态字段清晰、索引覆盖查询路径、时间字段、乐观锁版本号（并发更新场景）。

14. **热点行更新怎么缓解？**  
    **答：** 拆分冷热聚合、批量更新、串行化队列、必要时换存储（如 Redis 计数周期性落库）。

15. **缓存一致性怎么选策略？**  
    **答：** 读多写少用缓存；更新策略按业务容忍度（延迟双删/先 DB 后删缓存/canall/短 TTL）；强一致少依赖缓存。

16. **Redis 分布式锁注意什么？**  
    **答：** 原子加锁解锁、过期与续期（防死锁/误删）、Redlock 争议点了解即可。

### E. Kafka / MQ（3）
17. **Kafka vs 选型一句话？**  
    **答：** 高吞吐日志与事件流、可重放用 Kafka；复杂路由/传统业务异步解耦可看 RabbitMQ 等。

18. **消息堆积怎么排查？**  
    **答：** 看入栈出栈速率、消费滞后、消费者线程/批量、下游慢 SQL、broker 磁盘与副本。

19. **死信队列什么时候用？**  
    **答：** 业务拒绝/不可恢复错误；可恢复问题用限次重试+退避；死信要告警与人工补偿入口。

### F. Docker / K8s / 观测（3）
20. **滚动发布如何保证可用？**  
    **答：** readiness 通过才接流量；先起新后停旧；回滚上一版本；观测错误率与耗时。

21. **liveness 和 readiness 区别？**  
    **答：** readiness：能否接流量；liveness：进程是否该重启；配错会误杀或流量打到未就绪实例。

22. **线上故障你先看什么？**  
    **答：** 错误率/延迟、依赖超时、资源 CPU/内存/GC、DB 慢查询、MQ 滞后、最近发布。

### G. 阿里云（2）
23. **你用过哪些云产品解决什么问题？**  
    **答：** SLB 入口、RDS/OSS、Redis、SLS 日志、监控告警（按真实经历说）。

24. **云上数据库连接被打满怎么判与解？**  
    **答：** 看连接数与慢 SQL；池化、限流、读写分离、优化 SQL、扩容。

### H. 物联网协议（3）
25. **MQTT QoS0/1/2？**  
    **答：** 0至多一次，1至少一次，2恰好一次更贵；一般业务上报 0/1，关键指令看成本选 1/2。

26. **Topic 怎么分层？**  
    **答：** product/device/属性或事件类型/上下行分离；避免爆炸与难治理。

27. **指令下发超时怎么处理？**  
    **答：** 超时状态机、可重试策略、设备离线队列/补偿、幂等与可观测。

### I. 深挖简历综合（2）
28. **AIoT 你解决过哪些线上问题？**  
    **答：** 重复上报、解析失败、链路异常；手段：校验、物模型版本、幂等、监控对账、补数策略。

29. **RPA 云控你最得意的能力是什么？**  
    **答：** 0-1 核心模块串联：任务调度+开放 API+监控日志+商业化许可对接。

30. **GPT 平台异步与计费你怎么讲？**  
    **答：** taskId 追踪；流式按片段累计 token；异常重试与冲正口径；对账兜底。

---

## 二、场景题（由浅入深）— AIoT / MQTT / 物模型

| 层级 | 场景题 | 简答（背要点） |
|------|--------|----------------|
| 浅 | 设备第一次上线要完成哪些步骤？ | 注册/绑定→鉴权→激活→物模型版本对齐→订阅 Topic→开始上报。 |
| 浅 | 上报数据为什么要过物模型？ | 统一字段语义与校验；便于不同机型扩展；前后端协议稳定。 |
| 中 | 上报包字段缺失/类型不对你怎么处理？ | 入隔离/死信；告警；不污染主链路；统计驱动治理厂商固件。 |
| 中 | 重复上报怎么保证幂等？ | 唯一 msgId/业务序列；DB 唯一键；重复则跳过或只更新必要字段。 |
| 深 | “消息丢失”你怎么证明与定位？ | 对比接入量与落库量；分桶时间序列；查 broker/客户端重连；抽样对账；必要时补采。 |
| 深 | 指令下发与设备回执不一致怎么办？ | 明确状态机（已下发/已确认/执行成功失败/超时）；超时重试策略；以幂等指令为准；人工介入通道。 |

---

## 三、场景题 — 金蝶 RPA 云控台（调度 / API / 许可）

| 层级 | 场景题 | 简答 |
|------|--------|------|
| 浅 | 云控台核心解决用户什么问题？ | 管流程/机器人/任务、可看执行状态与日志、能调度与对接业务系统。 |
| 浅 | Open API 一般要提供什么能力？ | 触发任务、查状态、拉日志/回调、鉴权限流。 |
| 中 | 任务重复触发如何避免重复执行？ | 业务幂等键；任务实例维度去重；状态机禁止重复进入 RUNNING。 |
| 中 | 回调到达顺序乱/重复怎么办？ | 以任务实例 ID 幂等；事件按序号或版本推进；乱序不入终态。 |
| 深 | 资源控制/许可证如何做“限流公平”？ | 租户配额、并发上限、队列优先级；令牌/许可校验在调度前；拒绝策略可读错误码。 |
| 深 | 大客户端集成你如何降低对接成本？ | 稳定 API 版本、明确错误码、沙箱、示例 SDK（如有）、回调重放说明。 |

---

## 四、场景题 — GPT 平台（异步 / 流式 / Token）

| 层级 | 场景题 | 简答 |
|------|--------|------|
| 浅 | 为什么要统一模型网关？ | 多厂商差异收口；密钥与路由；监控计费与降级。 |
| 浅 | 同步 vs 异步分别适用？ | 短请求同步；长任务/批处理异步+轮询/回调。 |
| 中 | 流式接口如何记录 token？ | 按 chunk 累计；结束帧结算；异常断开按已定口径计费并标记。 |
| 中 | 调用成功但扣费失败怎么处理？ | 账务冲正/重试；账务幂等；最终对账；人工差错单。 |
| 深 | 如何避免“并发调用超配额”？ | Redis 计数+Lua/原子校验；配额与审批流；429+退避重试。 |
| 深 | 观测侧你最关心哪些指标？ | QPS、P95 延迟、错误类型、各模型成功率、token 消耗、队列积压。 |

---

## 五、背诵提示（答题骨架）

- **先说模块**：接入 / 调度 / 交易 / 观测 / 商业化。  
- **再说工程**：幂等、状态机、MQ 最终一致、对账、限流熔断、可观测。  
- **最后落到简历**：AIoT（MQTT+物模型+链路问题），RPA（调度+API+许可），GPT（异步流式+token）。

如果你希望把上面 **30 道题改成「只有 JD 设备云」或「只有 Java 栈」两套卷**，可以说明一下，我帮你拆成两张清单便于轮换练习。