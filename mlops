1. 一般 MLOps 系统是干什么的？
MLOps（Machine Learning Operations） 把机器学习从「实验室脚本」变成「可重复、可观测、可发布、可回滚的工程体系」。典型目标包括：

数据与特征：采集、清洗、版本化、血缘可追溯
训练与实验：任务编排、资源调度、参数与代码版本对齐
模型与发布：注册、审批、灰度、A/B、回滚
评测与监控：离线指标、在线效果、漂移与数据质量告警
治理与安全：权限、审计、合规、成本与配额
一句话：让模型像软件一样可运维，并且把「数据 → 训练 → 评测 → 上线 → 反馈再迭代」连成闭环。

2. 你们平台里这些功能在干什么？解决什么业务？
结合你们侧栏与「工作台」看，这是面向 扫地机器人（端侧感知/导航/避障等） 的 数据飞轮 + 训练运维 平台，大致可对应到下面业务问题。

工作台（总览）
计算任务：训练/评测/批处理等任务排队、运行、成功失败——回答「算力侧 pipeline 是否健康」。
计算资源：节点、GPU 利用率——回答「资源是否够用、是否浪费」。
ETL 三段（AIoT 消息 → H265 合 Bag → 预处理出图）：这是扫地机场景里很典型的一条链路——回答「端上回传的压缩视频/分包消息，是否在云端被稳定消费、积压是否恶化」。
H265：端上常见的高效视频编码，适合带宽与存储成本。
Bag：机器人圈常用的 ROS bag 一类「可回放传感器时序容器」，便于对齐多传感器时间线。
预处理：从 bag/视频流里抽帧、筛选、标准化，为标注与训练准备「可用图片/样本」。
数据回传（从机器到云）
解决：海量终端如何把“有价值现场数据”可靠、可控、可追踪地送回云端。

回传控制中心：策略（网络条件、触发条件、采样率、隐私/合规开关等）。
回传数据列表：已到达云端的原始/分包数据台账。
合包任务列表：把小碎片合并成可处理的大包，降低 IO 与任务调度开销。
回传链路检索：端到端排障（哪一段慢、哪一段失败、是否丢包/重试）。
业务价值：把「售后/运营/研发看到的现场问题」变成 可复现的数据资产，而不是零散文件。

数据管理（从原始到可训练）
解决：数据如何被组织成“可检索、可版本、可组合”的训练输入。

数据源：不同机型、固件、地区、项目线的数据入口治理。
Bag 管理：管理机器人录制包（多传感器对齐、回放、切片）。
数据集：把样本划分/组合成训练、验证、测试集，并与任务绑定。
标注管理（人机协同）
解决：模型需要监督信号时，如何把标注产能规模化、标准化、可质检。

图片/视频/文本标注：不同模态与任务（检测、分割、深度辅助、日志分类等）。
标注场景：按场景分层（光照、地面材质、杂物类型等），提升模型泛化与问题归因。
标注标签：标签体系统一，避免「同名不同义」导致训练噪声。
任务管理 / 训练管理 / 模型管理 / 评测分析 / 存储管理
这一组解决：从实验到生产的“流水线化”。

任务列表 / DAG 组件 / 模板 / 触发：把数据处理、训练、评测固化成可复用流水线，并用定时/事件触发自动化。
在线 IDE / 任务建模：降低研发门槛，统一训练配置与代码环境。
训练模型 vs 生产模型：区分「还在迭代」与「已发布可用」，对应版本治理与发布门禁。
评测记录：同一模型多轮对比、回归分析、上线依据沉淀。
Bucket / S3 用户：对象存储承载原始数据、特征、模型产物；权限与租户隔离。
系统设置
解决：项目隔离、配置一致性、操作可追溯（审计）。

项目配置：资源配额、默认存储、环境变量、集成参数等。
操作记录：谁在何时改了什么（对线上事故复盘很关键）。
3. 扫地机器人业务上，这条链路到底在闭环什么？
用业务语言概括：扫地机在真实家庭里会遇到 长尾障碍物、反光地面、低矮家具、线材、宠物场景 等；平台做的是：

有选择地回传现场传感数据（控制成本与合规）
云端标准化成可训练格式（bag/图片流水线）
标注与数据集版本化沉淀监督学习样本
训练与评测迭代感知/深度/语义/匹配等模型族
发布到生产并在真机上回归（你们仪表盘里也有「真机评测」这类指标导向）
线上问题再触发回传与迭代（飞轮）