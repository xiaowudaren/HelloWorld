
下面按 **岗位 JD（机器人云平台 / 机械臂、Go+Python、阿里云、MySQL/Redis/MQ、Docker/K8s、MQTT 等加分项）** 和你简历里的 **强相关点（任务调度、设备/物模型/MQTT、订单售后物流支付、微服务、RPA 控制台、GPT 计量调度）** 整理：**可快速背诵的问答** + **从项目往细节追问的路径**。

---

## 一、岗位“主干题”（高概率）

### 1. 为什么是 Go？Go 和 Python 你怎么选？
**答（短）：**  
- **Go**：高并发、部署简单、性能好，适合做 **网关、调度、设备接入旁路服务、MQ 消费、长连接旁路** 等。  
- **Python**：生态强，适合做 **脚本工具、算法侧胶水、Agent/数据处理**。  
- **原则**：对延迟与并发敏感的核心链路优先 Go；快速集成与 AI/数据处理优先 Python。

### 2. 微服务拆分原则（你们场景里怎么说）
**答（短）：**  
按 **领域边界 + 变更频率 + 数据一致性边界** 拆：设备管理、任务调度、文件/日志、订单支付售后等各自独立；共享能力下沉为 **基础组件**（鉴权、配置、消息规范、幂等、审计）。

### 3. MySQL：你怎么做表设计与索引？
**答（短）：**  
- 先定 **查询路径**，再定索引；高基数字段更适合索引。  
- **避免**：过长索引、过多联合索引、大表全表扫。  
- **一致性**：核心业务用事务；跨服务用 **最终一致性（MQ/TCC/对账）**。

### 4. Redis：缓存雪崩/击穿/穿透怎么处理？
**答（短）：**  
- **雪崩**：过期时间加随机抖动；热点不过期或异步刷新；限流降级。  
- **击穿**：热点 key 互斥锁/逻辑过期/永不过期+异步刷新。  
- **穿透**：布隆过滤器；不命中也缓存空值（短 TTL）。

### 5. Kafka / RabbitMQ：你怎么选？怎么做可靠性？
**答（短）：**  
- **Kafka**：高吞吐日志、事件流、可重放、消费者扩容。  
- **RabbitMQ**：路由灵活、延迟/优先级场景（看版本与架构）。  
- **可靠性**：生产者 ack、消费者 ack、幂等键、死信队列、重试退避、可观测（积压监控）。

### 6. Docker & K8s：你们如何发布与回滚？
**答（短）：**  
- 镜像固定 tag；滚动发布；健康检查（readiness/liveness）；  
- 回滚：**上一版本 Deployment** 或金丝雀/蓝绿（看团队规范）；  
- 关键：**配置外置、可灰度、可观测（日志指标链路）**。

### 7. 阿里云经验：你用过哪些？各自解决什么？
**答（短，按需说你会的）：**  
- **SLB/ALB**：入口流量与伸缩；  
- **RDS**：托管 MySQL；  
- **Redis**：缓存；  
- **OSS**：文件/日志归档；  
- **MQ**：消息；  
- **日志服务 SLS + 监控 ARMS/云监控**：排障与容量。

> 若你简历里阿里云细节不多，这句话很稳：**“云上以托管服务降运维成本，关键在配额、网络与安全组、限流与观测。”**

### 8. gRPC / MQTT / WebRTC 加分项（你没强项也别硬装）
**答（短）：**  
- **gRPC**：二进制、强类型、流式；适合服务间低延迟调用（注意网关、超时、重试幂等）。  
- **MQTT**：轻量、适合物联网上下行；Topic 规划、QoS、会话与重连、上行风暴治理。  
- **WebRTC**：音视频中低延迟；信令服务器 + ICE/TURN（你可以说“了解范式”，除非你真做过）。

---

## 二、机械臂/机器人云平台“场景题”（从 JD 反推）

### 9. 机器人设备管理一般要管什么？
**答（短）：**  
设备注册/鉴权、在线状态、固件版本、能力集、SN、绑定关系、告警、远程运维通道（策略与审计）。

### 10. 任务调度中心在云侧要解决什么？
**答（短）：**  
任务建模（优先级/截止时间/依赖）、下发与确认、重试与幂等、状态机、可观测（排队、失败原因）、和资源占用（避免热点设备过载）。

### 11. 订单/售后/物流/支付在后端怎么保证不乱？
**答（短）：**  
- 每个订单状态 **明确状态机**；  
- 外部回调 **幂等**；  
- 资金相关 **对账**；  
- 跨系统用 **MQ + 补偿**；异常可追踪（单号贯穿）。

### 12. ROS/ROS2（加分）：你了解多少？
**答（短）：**  
了解它是机器人中间件与消息通信框架；云侧更多关注 **与现场系统的接口（MQTT/gRPC/自定义 bridge）** 而不是云端直接跑完整 ROS（除非你做过）。

---

## 三、面试官从你简历“项目链条”逐级追问（问题 + 简短答案）

下面用“**先粗后细**”的追问链，你按项目准备即可。

### A. 云鲸/现公司：AIoT 平台（和你投递岗位最贴近）

**A1：你在 AIoT 里具体做了什么？**  
**答：** MQTT 消息接入处理、payload 校验解析、物模型映射、异常与持久化、远端指令下发与回执、在线离线监控、线上问题（丢包/重复/解析失败）。

**A2：MQTT Topic/QoS 怎么设计？**  
**答：** Topic 按 `product/device/event` 分层；区分属性上报/事件/指令；QoS0/1/2按成本与可靠性选择；指令通常要可追踪（reqId）。

**A3：物模型不一致怎么办？**  
**答：** 版本化管理；上报字段校验；缺失/类型不匹配进入异常通道与告警；必要时 **降级策略**（不入库或入原始通道）。

**A4：重复消息怎么处理？**  
**答：** **幂等键**（deviceId+timestamp+msgId）；落库唯一约束；业务层去重。

**A5：线上怎么排查“消息丢了/少了”？**  
**答：** 对比接入量与落库量；链路日志；QoS；补偿任务；监控突降；看 broker 集群与客户端重连日志。

---

### B. 金蝶：苍穹 RPA 云控台（任务调度/机器人管理强相关）

**B1：RPA 云控台和真·机械臂云平台有什么共同点？**  
**答：** 都有 **机器人实例、任务编排、调度、执行状态、日志追踪、开放 API/回调**；差别在物理侧多了时延/安全/硬件约束。

**B2：任务调度你怎么保证不重不漏？**  
**答：** 任务状态机；调度前先抢锁/版本号；执行结果回调幂等；失败重试可配置；死信与人工介入。

**B3：开放 API 设计你关注什么？**  
**答：** 鉴权、限流、幂等、签名回调、可观测、向后兼容；错误码清晰。

---

### C：金蝶 GPT 平台（可映射到“智能调度/大模型网关”叙述）

**C1：你做 GPT 平台哪个模块？**  
**答：** 模型实例管理、统一调用、同步/异步/流式、监控统计、**token 计费与配额**。

**C2：异步任务怎么做状态追踪？**  
**答：** taskId；任务表状态机；回调/Webhook；重试；失败原因归档。

**C3：流式调用要注意什么？**  
**答：** 超时、背压、断开续传策略（看产品）、计费切片、并发连接数限制。

---

### D：供应链中台 / EAM 资产（对应 JD 里订单售后物流支付）

**D1：你用过哪些“计划/承诺”概念（MPS/ATP）？**  
**答：**  
- **MPS**：主生产计划排产；  
- **ATP**：可承诺量，用来答复交期；  
重点是 **供需平衡** 与规则版本化，不是背公式。

**D2：EAM 里资产闭环你怎么讲？**  
**答：** 申请-审批-采购-出库-SN-验收-转固/财务入账-调拨回收；和 SAP/OMS/WMS 打通，关键在 **主数据一致性与状态机**。

**D3：对接外部系统最容易出什么事？**  
**答：** 回调重复、字段变更、时区金额单位、对账不一致；需要 **幂等+补偿+审计日志**。

---

### E：低代码表单 + 自研工作流

**E1：自研工作流引擎难点在哪？**  
**答：** 流程定义版本、运行实例一致性、回退/驳回、并发审批、与业务表单数据绑定。

**E2：动态表结构你怎么设计（简述）**  
**答：** JSON Schema/元数据表 + 通用扩展表或 EAV（权衡查询）；索引策略与归档。

---

## 四、你相对 JD 的“主动解释话术”（30 秒版本，建议背）

**答：**  
“我后端 9 年，做过 **企业级平台与复杂集成**。最近一段在机器人公司做过 **AIoT：MQTT、物模型、远程控制与监控**，也做过 **供应链/资产/流程** 这类强状态机业务。技术栈我主 **Java 微服务**，同时 **熟悉 Go**，日常用 **Python 做 Agent/编排**。对 **MySQL/Redis/RabbitMQ** 有线上经验，理解和云原生部署与可观测这一套。若岗位偏 Go 与阿里云，我能把设备接入与调度侧的经验迁移过来，并快速补齐阿里云与 K8s 的工程细节。”

---

## 五、反向追问面试官（可选，显得你懂业务）

- 设备侧链路：**MQTT 还是自研长连接？是否还有边缘网关？**  
- 调度：**任务粒度是脚本级还是更细的动作序列？SLA 是多少？**  
- 观测：是否 **OTel/链路追踪** 统一到一台？故障演练怎么做？

---

如果你希望我把它做成**一张“面试小抄表”**（例如按：基础/中间件/云/物联网/项目深挖分栏），你说一下：你更想突出 **Go** 还是 **Java 微服务** 作为主叙述（简历主优势仍是 Java，但 JD 主语言是 Go），我可以把表述再“对齐岗位口吻”收敛一版。
