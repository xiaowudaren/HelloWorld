
下面用「面试能说清 + 上手能照着做」的方式说明：**Java 与 Go 的核心差异**、**Go 的核心点**、**从 Java 转 Go 的思路**。

---

## 一、Java 与 Go 的核心差异（对比记忆）

| 维度 | Java（常见企业栈） | Go |
|------|-------------------|-----|
| **执行模型** | JVM 字节码，JIT，跨平台强 | 编译成**单一静态二进制**，启动快，部署简单 |
| **并发** | 线程 + 线程池 + `java.util.concurrent`；协程有 Project Loom（视版本） | **goroutine + channel** 是一等公民，轻量并发写进日常代码 |
| **面向对象** | 经典 OOP：继承层次、`interface` 实现关系显式 | **组合优于继承**；`interface` **隐式实现**（duck typing） |
| **错误处理** | `try/catch`，异常可栈追踪 | **显式 `error` 返回值**，没有异常体系（需习惯 `if err != nil`） |
| **泛型** | 成熟泛型 | Go 1.18+ 有泛型，但**日常风格**仍偏简单类型与组合 |
| **依赖管理** | Maven/Gradle，生态巨无霸 | **Go Modules**（`go.mod` / `go.sum`），标准库强、三方精 |
| **生态典型用途** | 大型企业应用、Spring 全家桶 | **云原生、CLI、网关、中间件、高并发 IO、工具链** |
| **空值** | `null` | **零值**（数字 0、bool false、指针 nil、slice/map 可用但注意 nil） |

**一句话面试版：**  
Java 强在**业务框架与生态**；Go 强在**简单语法、静态链接、高并发与工程交付速度**。机器人/云平台后端里，很多服务用 Go 做**接入、转发、调度旁路**，并不奇怪。

---

## 二、Go 的核心点（必须建立的心智模型）

### 1. 并发三件套
- **goroutine**：`go func()` 开协程，极廉价。
- **channel**：协程间传递数据、`range` 消费、带缓冲/不带缓冲、关闭语义。
- **同步原语**：`sync.Mutex`、`sync.RWMutex`、`sync.WaitGroup`、`context`（取消与超时贯穿调用链）。

**要点：** Go 推荐「**用 channel 传消息，用锁保护共享内存**」按场景选，不要只会一种。

### 2. `interface` 与组合
- **小接口**（如 `io.Reader`）、**隐式满足**，利于测试（mock）。
- 用 **struct 嵌入** 做组合，而不是深继承树。

### 3. 错误与 panic
- **正常错误**：`return err`，调用方处理。
- **panic**：一般视为 bug 或不可恢复；`recover()` 只在少数场景（如 HTTP 中间件顶层兜一下）。

### 4. 值语义 vs 指针
- **slice、map、channel、interface、func** 等本身是**引用语义**（底层共享）。
- **struct** 传值还是 `*struct`：大对象、需修改原对象、接口实现细节时要注意指针接收者。

### 5. 工程与工具链（算 Go 的“卖点”）
- `gofmt` 统一格式、`go test` 内置测试、`go vet`、benchmark、`pprof` 性能分析。
- 强调**简单、可读、少魔法**，和大型 Java 注解堆叠是不同味道。

### 6. 和 Java 最不一样的编码习惯
- **没有构造函数约定**：常见 `NewXxx()` 返回指针。
- **defer**：清理资源（注意 defer 在 return 前后执行顺序）。
- **包可见性**：首字母大写导出，小写包内可见。

---

## 三、从 Java 转 Go：推荐思路（按阶段）

### 阶段 1：语法 + 习惯（1～2 周，每天写一点）
- 过一遍：类型、slice/map、方法、interface、`error`、`defer`、`goroutine/channel`、`context`。
- **刻意练习**：把 Java 里「线程池任务」改写成「多个 goroutine + WaitGroup/channel」。

### 阶段 2：并发与 IO（最重要）
- 搞懂：**何时用 mutex，何时用 channel**；`context` 取消在多层函数里怎么传。
- 写小项目：**HTTP 客户端限流**、**并发抓取**、**worker pool**（这和调度/消费 MQ 很像）。

### 阶段 3：工程化对齐你熟悉的 Java 世界
| Java 概念 | Go 里怎样对齐 |
|-----------|----------------|
| Spring Boot Web | **标准库 `net/http`**，或 **Gin / Echo / Fiber**（选一个学透） |
| Spring DI | **手写注入**、小规模用 **wire**（进阶） |
| MyBatis / JPA | **database/sql** + **sqlx** 或 **GORM**（先会一种） |
| Config Server | **viper**、环境变量、启动参数（云原生常见） |
| 日志 | **zap** / **zerolog**（结构化日志） |
| gRPC | **官方 grpc-go**，proto 代码生成 workflow 跑通一遍 |

### 阶段 4：读代码比啃书快
- 找 **小而干净** 的开源：如标准库小工具、知名中间件的 client 部分。
- 面试若强调 Go：准备 **1 个可演示的小服务**：REST + MySQL + 健康检查 + 优雅退出（`context` + `Shutdown`）。

### 阶段 5：心理转换（面试官也爱听）
- **少框架依赖**，多显式逻辑；接受 `if err != nil` 啰嗦，用 helper/linter 缓解。
- **先正确再优雅**：并发里**数据 races** 很致命，学会用 `go test -race`。

---

## 四、面试时怎么诚实又加分（你不熟 Go 时）

可以这样讲：  
「我主力是 **Java 微服务**，对 **并发与分布式问题（幂等、MQ、缓存、数据库事务）** 是相通的。Go 我按计划补齐 **语法、goroutine/channel、HTTP/gRPC、modules**，并通过 **一个小服务 + race 检测** 验证。**语言是工具，工程经验可迁移。**」

若对方硬考 Go：最常问 **`defer` 执行顺序**、**slice 扩容与共享底层数组陷阱**、**map 并发读写**、**goroutine 泄漏与 context 取消**——提前各准备一句即可。

---

如果你愿意，可以说一下你目标时间（例如「两周后要面」），我可以按**每日 1 小时**给你排一个最小学习清单（只列课题不写代码——当前是 Ask 模式；若要我在仓库里加练习项目再切 Agent 模式）。
